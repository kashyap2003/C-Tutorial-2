#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    double gpa;
    printf("Enter your gpa: ");
    scanf("%lf", &gpa); // * Remember you have to put lf in palce of 'f' to get your required result.
    printf("Your gpa is %f", gpa);
}